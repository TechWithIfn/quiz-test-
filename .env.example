# Optional public site URL used by client-side configuration.
# The application works normally when this is left blank.
VITE_SITE_URL=https://your-real-domain.com

# Optional backend API base URL. Leave blank to use same-origin "/api".
# Used only once the backend (backend/) is deployed and wired in.
VITE_API_BASE_URL=
